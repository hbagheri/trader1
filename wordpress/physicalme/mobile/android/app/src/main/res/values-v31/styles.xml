<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31+) splash. The OS shows this BEFORE our launch theme.
     windowSplashScreenBackground sets the full-screen color; the launcher icon
     foreground is centered on top of it. -->
<resources>
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splashBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
